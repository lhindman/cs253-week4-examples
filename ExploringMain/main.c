#include <stdio.h>
#include <stdlib.h>

int main(void) {

   /* A non-zero exit code indicates that an error has occurred */
   // return 1;
   // exit(1); 

   /* An exit code of zero indicates that the program ran successfully */
   return 0; 
   // exit(0);
}
